import WikiPage from './WikiPage';

export default class PageCrawlerImpl {

    static getInheritedPage(SUITE_SETUP_NAME: any, wikiPage: WikiPage) {
        return null;
    }

    addPage(root: WikiPage, parse: string, content: string) {
        return undefined;
    }
}